@model Constellation.Presentation.Staff.Areas.Staff.Pages.Shared.PartialViews.RemoveTeacherFromTutorialModal.RemoveTeacherFromTutorialModalViewModel

<form asp-page-handler="RemoveTeacher" method="post">
    <div class="modal-header">
        <h5 class="modal-title">Tutorial Teacher Removal</h5>
    </div>
    <div class="modal-body">
        <div class="row">
            <div class="form-group col">
                <span class="control-label">Staff Member</span>
                <input asp-for="Id" type="hidden" />
                @Model.Name
            </div>
        </div>

        <div class="row">
            <div class="form-group col-6">
                <span class="control-label">Remove Immediately?</span>
            </div>
            <div class="form-group col-6">
                <input asp-for="Immediate" class="form-control" />
            </div>
        </div>

        <div class="row">
            <div class="form-group col-6">
                <span class="control-label">Or at specific date?</span>
            </div>
            <div class="form-group col-6">
                <input asp-for="EffectiveOn" class="form-control" type="date" />
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <input type="submit" value="Save" class="btn btn-primary" />
        <button type="button" class="btn btn-secondary" data-dismiss="modal" data-bs-dismiss="modal">Close</button>
    </div>
</form>
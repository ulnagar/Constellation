@model Constellation.Presentation.Staff.Areas.Staff.Pages.Shared.Components.TutorialRollAddStudent.TutorialRollAddStudentSelection

<div class="modal fade" tabindex="-1" id="rollAddStudentModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-page-handler="AddStudent">
                <div class="modal-header">
                    <h5 class="modal-title">Tutorial Roll Add Student</h5>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="form-group col">
                            <span class="control-label">Student</span>
                            <select asp-for="StudentId" asp-items="@(new SelectList(Model.StudentList, "Key", "Value"))" class="combo" style="width:100%">
                                <option value="">-- Select --</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <input type="submit" value="Save" class="btn btn-primary btn-block btn-lg showLoader" />
                    <button type="button" class="btn btn-secondary" data-dismiss="modal" data-bs-dismiss="modal">Close</button>
                </div>
            </form>
        </div>
    </div>
</div>
@{
    Layout = "/Pages/Shared/_WebLayout.cshtml";

    string activePage = ViewData["ActivePage"].ToString();
}

<div class="row sidebar-layout">
    <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
        <div class="sidebar-sticky pt-3">
            <h4 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mb-1 text-muted">
                <span class="w-100">Assets</span>
            </h4>
            <ul class="nav flex-column mx-2">
                <li class="nav-item">
                    <a class="nav-link @(activePage == AssetsPages.Assets ? "active" : "")" asp-page="/Assets/Index" asp-area="Equipment">
                        Assets
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(activePage == AssetsPages.Allocations ? "active" : "")" asp-page="/Allocations/Index" asp-area="Equipment">
                        Allocations
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(activePage == AssetsPages.Locations ? "active" : "")" asp-page="/Locations/Index" asp-area="Equipment">
                        Locations
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(activePage == AssetsPages.Reports ? "active" : "")" asp-page="/Reports/Index" asp-area="Equipment">
                        Reports
                    </a>
                </li>
            </ul>
        </div>
    </nav>
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4 pt-md-3">
        @RenderBody()
    </main>
</div>

@section scripts {
    <script>
        $(document).ready(function () {
            var container = $('#main-container');
            container.removeClass('container');
            container.addClass('container-fluid');
            
            var div = $('#main-container div:first-child');
            div.removeClass('pb-3');

            $('html, body').css({
                overflow: 'hidden',
                height: '100%'
            });
        });
    </script>

    @await RenderSectionAsync("scripts", required: false)
}
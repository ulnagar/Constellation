@page
@model Constellation.Presentation.Server.Areas.Test.Pages.IndexModel

<h2>Test Page</h2>

<ul>
    @foreach (var entry in Model.Courses.OrderBy(entry => entry.CourseCode))
    {
        <li>@entry.CourseCode : @entry.Name</li>
    }
</ul>
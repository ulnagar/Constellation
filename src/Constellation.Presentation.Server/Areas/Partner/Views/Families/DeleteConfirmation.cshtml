@model DeleteConfirmation
@using Constellation.Presentation.Server.Areas.Partner.Models.Families;

<div class="modal-header">
    <h5 class="modal-title">@Model.Title</h5>
</div>
<div class="modal-body">
    <div class="row">
        <div class="form-group col">
            <span class="control-label">Are you sure you wish to remove @Model.UserName from the @Model.FamilyName family?</span>
        </div>
    </div>
</div>
<div class="modal-footer">
    <a href="@Model.Link" class="btn btn-danger">Remove</a>
    <button type="button" class="btn btn-secondary" data-dismiss="modal" data-bs-dismiss="modal">Close</button>
</div>
